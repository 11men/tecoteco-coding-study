spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/${MYSQL_DATABASE:busapp}?useSSL=false&allowPublicKeyRetrieval=true
    username: ${MYSQL_USER:busapp}
    password: ${MYSQL_PASSWORD:busapp123}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080
